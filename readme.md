## 1. Анализ временных рядов
 - Вычисление скользящего среднего и стандартного отклонения для сглаживания температурных колебаний.
 - Определение аномалий на основе отклонений температуры от  скользящее среднее±2𝜎 .
 - Построение долгосрочных трендов изменения температуры.

 Реализовано в **EDA.ipynb**

 ## 2. Паралелльный анализ данных

Сравнение классического **pandas** c:

 - **pandas с многопроцессностью (multiprocessing)**

 Использует стандартный pandas, распараллеливая операции по строкам или столбцам с помощью multiprocessing.

 - **modin**

 Альтернатива pandas, автоматически распараллеливающая операции с использованием Dask.

 - **polars**

 Высокопроизводительная библиотека для обработки данных с помощью многопоточности и оптимизаций на уровне алгоритмов.

Реализовано в файле __parallel_EDA.py__

### __Результаты:__

| Подход                 | Median (s) | Min (s) | Max (s) | Runs (s)                               | Вывод относительно **default pandas**                                                                               |
| ---------------------- | ---------: | ------: | ------: | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| default pandas         |     0.0204 |  0.0203 |  0.0306 | 0.0306, 0.0203, 0.0204, 0.0204, 0.0205 | База: самый быстрый и стабильный результат в этом тесте.                                                            |
| pandas multiprocessing |     1.1015 |  1.0902 |  1.1589 | 1.1589, 1.1015, 1.0938, 1.0902, 1.1062 | **≈54× медленнее по median** (1.1015 / 0.0204). Накладные расходы на процессы перекрыли выгоду.                     |
| modin                  |     1.1880 |  0.9517 |  3.3438 | 3.3438, 1.0009, 1.7460, 1.1880, 0.9517 | **≈58× медленнее по median**. Сильная нестабильность (max 3.3438s) — вероятно, из-за overhead/планирования задач.   |
| polars                 |     0.0278 |  0.0267 |  0.1163 | 0.1163, 0.0303, 0.0278, 0.0267, 0.0273 | **≈1.36× медленнее по median**. В целом близко к pandas, но есть выброс (0.1163s), из-за которого max заметно выше. |


### __Выводы:__

На наших данных достаточно использовать стандартный `pandas`, который показал высокую эффективность и оказался быстрее некоторых параллельных подходов за счёт отсутствия временных расходов для организации параллельности. 

Использование `multiprocessing` с pandas привело к резкому ухудшению производительности из-за затрат на создание процессов и управление процессами. 

`Modin` на `Dask` не дал выигрыша по времени, так как накладные расходы на планирование вычислений превышают пользу от параллелизма на небольших данных. 

Быстрее всех справился `Polars`, обеспечив максимальную скорость и стабильность, благодаря встроенной многопоточности и оптимизированной обработке данных.

## 3. Отправка запросов в OpenWeatherMap API (синхронно/асинхронно)

Сравнение подходов при отправке запросов OpenWeatherAPI по всем городам в выборке:

 -  Синхронно с помощью библиотеки **requests**.
    Время работы синхронного варианта: 1.52 с

 -  Асинхронно с помощью **asyncio** и **aiohttp**.
    Время работы aсинхронного варианта: 0.21 с

Реализовано в файле **weather_req.py**
Результаты можно увидеть в папке `/data`.

### __Выводы:__

При отправке запросов в OpenWeatherAPI по всем городам сразу (т.е. много одновременных I/O Bound запросов) выгодно использовать асинхронный подход, т.к. он дает сильный выигрыш по времени. Тем не менее в рамках сервиса, который необходимо реализовать, будет отправляться запрос на получение текущей температуры только по одному городу. При таком техническом задании достаточно использовать синхронный подход, т.к. одномременно будет отправляться только 1 запрос на I/O. Асинхронность будет избыточна.

## 4. Streamlit app

В рамках данного пункта было разработано веб приложение с помощью Streamlit, которое:

 - Позволяет загрузить таблицу с историческими температурными данными или использовать внутреннюю. 
 - Позволяет выбрать город для анализа из выпадающего списка доступных, которые есть в исторических данных.
 - Обрабатывает исторические данные для выбранного города и выводит статистики по сезонам и месяцам, а также погодные тренды.
 - Все графики интерактивные. Для помощи в оформлении графиков с помощью **plotly** использовалась LLM (ChatGpt 5.2 free)
 - Запрашивает API ключ и синхронно отправляет запрос в OpenWeatherMap для получения реальной температуры и ее классификации (аномалия или нет).
 - Возвращает ошибки в формате **{"cod": num, "message": "text"}**, которые могли бы произойти при отправке запроса в API.

 Реализовано в файле **app.py**